<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users Data</title>
    <link rel="stylesheet" href="css/output.css">
    <!-- <script src="https://cdn.tailwindcss.com"></script> -->
</head>
<body>
    <div class="bg-zinc-900 w-screen min-h-screen text-white p-10">
        <a href="/" class="text-blue-500 ">Go to home</a>
        <h1 class="text-3xl mb-4 mt-4">All User</h1>
            <div class="users flex gap-4 flex-wrap">
                
                <% if (userRead.length > 0) { %>
                    <% userRead.forEach(user => { %>
                        <div class="user w-64 bg-zinc-700 p-5 rounded-md">
                            <div class="w-full h-52 bg-zinc-800 rounded-md">
                                <img src="<%= user.image %>" class="w-full h-full object-cover object-center">
                            </div>
                            <h1 class="text-center text-xl mt-2"><%= user.name %></h1>
                            <p class="text-center text-zinc-400 mt-2"><%= user.email %></p>
                            <div class="links flex justify-around mt-2">
                                <a href="" class="text-blue-500">Edit</a>
                                <a href="/user/<%= user._id %>" class="text-red-500">Delete</a>
                            </div>
                        </div>
                    <% }) %>
                <% }  else { %>
                    <h1 class="text-zinc-400">No User Found</h1>
                <% } %>

                
            </div>
    </div>
</body>
</html>